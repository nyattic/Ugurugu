{
  "name": "ugurugu-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync-engine": "node sync-engine.mjs",
    "dev": "npm run sync-engine && vite",
    "build": "npm run sync-engine && vite build",
    "preview": "vite preview",
    "test:browser": "node tests/browser-verify.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "playwright-core": "^1.62.1",
    "svelte": "^5.0.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
