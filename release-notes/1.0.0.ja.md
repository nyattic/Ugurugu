ベータを終えて1.0になりました。これまでバグを報告してくださった
みなさまに感謝します。

## 改善

- 大きなキャンバスで描いているときに画面プレビューが使うメモリを、
  一つの予算の中で管理するようになりました。以前はプレビューキャッシュ
  が宣言した上限を超えることがあり、4096×4096のキャンバスを拡大して
  作業するとメモリが想定より増えていました。
- この変更により、4096×4096に近い大きなキャンバスを100%前後で表示した
  ときに、画面プレビューの解像度が少し下がります。保存や書き出しの画像
  には影響しません。
